[core]
    remote = storage
    autostage = true
['remote "storage"']
    url = s3://mba-mlops-bucket/
    endpointurl = http://minio:9000
    access_key_id = wrm
    secret_access_key = senha_segura
